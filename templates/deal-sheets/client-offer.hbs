<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{dealershipName}} — Your Vehicle Offer</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif; background: #f4f4f4; color: #333; }
    .wrapper { max-width: 600px; margin: 0 auto; background: #fff; }
    .header { background-color: {{brandPrimaryColor}}; padding: 32px 40px; text-align: center; }
    .header img { max-height: 50px; margin-bottom: 8px; }
    .header h2 { color: #fff; font-size: 20px; }
    .hero { padding: 40px; text-align: center; }
    .hero h1 { font-size: 24px; line-height: 1.3; color: #1a1a2e; margin-bottom: 12px; }
    .hero p { font-size: 16px; color: #666; line-height: 1.5; }
    .value-card { margin: 0 40px 32px; padding: 28px; background: #f9fafb; border: 2px solid {{brandPrimaryColor}}; border-radius: 12px; text-align: center; }
    .value-label { font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #888; margin-bottom: 6px; }
    .value-amount { font-size: 40px; font-weight: 700; color: {{brandPrimaryColor}}; }
    .equity-highlight { margin: 0 40px 32px; padding: 20px; background: #e8f5e9; border-radius: 10px; text-align: center; }
    .equity-highlight .label { font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #555; margin-bottom: 4px; }
    .equity-highlight .amount { font-size: 28px; font-weight: 700; color: #2e7d32; }
    .message { padding: 0 40px 32px; }
    .message p { font-size: 16px; line-height: 1.6; color: #444; margin-bottom: 16px; }
    .cta { padding: 0 40px 40px; text-align: center; }
    .cta a { display: inline-block; background: {{brandPrimaryColor}}; color: #fff; font-size: 18px; font-weight: 700; text-decoration: none; padding: 16px 48px; border-radius: 8px; }
    .cta a:hover { opacity: 0.9; }
    .footer { background: #333; padding: 28px 40px; text-align: center; }
    .footer .dealer-name { color: #fff; font-size: 16px; font-weight: 700; margin-bottom: 4px; }
    .footer .dealer-contact { color: #aaa; font-size: 14px; margin-bottom: 4px; }
    .footer .dealer-contact a { color: #aaa; text-decoration: none; }
    .footer .expiry { color: #888; font-size: 12px; margin-top: 12px; }
    .footer .disclaimer { color: #777; font-size: 11px; margin-top: 8px; line-height: 1.5; }
    @media (max-width: 640px) {
      .hero { padding: 28px 24px; }
      .hero h1 { font-size: 20px; }
      .value-card, .equity-highlight, .message, .cta { margin-left: 24px; margin-right: 24px; }
      .value-amount { font-size: 32px; }
      .footer { padding: 24px; }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <!-- Branded Header -->
    <div class="header">
      {{#if dealershipLogo}}
      <img src="{{dealershipLogo}}" alt="{{dealershipName}}">
      {{else}}
      <h2>{{dealershipName}}</h2>
      {{/if}}
    </div>

    <!-- Personalized Greeting -->
    <div class="hero">
      <h1>{{firstName}}, We Have Great News About Your {{year}} {{make}} {{model}}</h1>
      <p>We've evaluated your vehicle and have an exciting opportunity for you.</p>
    </div>

    <!-- Value Highlight -->
    <div class="value-card">
      <div class="value-label">Your Vehicle's Market Value</div>
      <div class="value-amount">{{marketValue}}</div>
    </div>

    {{#if showEquity}}
    <div class="equity-highlight">
      <div class="label">Your Estimated Equity</div>
      <div class="amount">{{equityAmount}}</div>
    </div>
    {{/if}}

    <!-- Opportunity Message -->
    <div class="message">
      <p>{{{clientMessage}}}</p>
    </div>

    <!-- CTA -->
    <div class="cta">
      <a href="{{dealershipWebsite}}/schedule-test-drive?ref={{token}}">Schedule a Test Drive</a>
    </div>

    <!-- Footer -->
    <div class="footer">
      <div class="dealer-name">{{dealershipName}}</div>
      <div class="dealer-contact">
        <a href="tel:{{dealershipPhone}}">{{dealershipPhone}}</a>
      </div>
      <div class="dealer-contact">
        <a href="{{dealershipWebsite}}">{{dealershipWebsite}}</a>
      </div>
      <div class="expiry">This offer is valid until {{expiresAt}}</div>
      <div class="disclaimer">This offer is an estimate based on current market data and is subject to in-person vehicle inspection. Values may vary based on vehicle condition.</div>
    </div>
  </div>
</body>
</html>
